#include <bits/stdc++.h>
#define MAX 100010
using namespace std;

int main(){
    int n, arr[MAX];
    cin >> n;
    for( int i{0}; i < n; ++i ){
        int tmp_read;
        cin >> tmp_read;
        arr[i] = tmp_read;
    }
    int maxSubsegment = 0;

    return 0;
}