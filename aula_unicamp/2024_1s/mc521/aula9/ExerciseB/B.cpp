#include <bits/stdc++.h>
using namespace std;

struct interval{
    int begin;
    int end;
};

vector<int> build_intervals( const vector<int>& numbers ){
    vector<int> intervals;
    
}

string solve( int n, int k ){
    vector<int> numbers;
    for( int i{0}; i < n; ++i ){
        int tmp;
        cin >> tmp;
        numbers.push_back(tmp);
    }

}

int main(){
    
}