#include <bits/stdc++.h>
#define MAX_N 200010
using namespace std;

int A[MAX_N];

long long num_divisible_subarrays( long long n ){
    
}

int main(){
    long long n;
    cin >> n;
    for( int i = 0; i < n; ++i ){
        long long tmp;
        cin >> tmp;
        A[i] = tmp;
    }
    

    return 0;
}