#include <bits/stdc++.h>

using namespace std;

// Preciso encontrar as substrings que são sufixo e prefixo e depois fazer a busca pela substring entre 1 até 
// n-2 (inclusivo)
int main( ){
    string str,
           preffix, suffix;
    cin >> str;
    for(int i{0}; i < str.length(); i++){
        
    }


    return 0;
}