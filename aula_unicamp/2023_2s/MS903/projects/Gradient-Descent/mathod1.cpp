#include<bits/stdc++.h>
#include<vector>

using namespace std;
using std::vector;

double f(double x)
{
    return (x*x);
}

double grad_f(double x)
{
    return (2*x);
}

double inftyNorm(vector<double> x)
{
    double max = x[0];
    for(int i{0}; i < x.size(); i++){
        if( x[i] > max ){
            max = x[i];
        }
    }
}

int main()
{
    double number{};
    while( cin >> number ){
        vector<double> number;
    }

    int M{0}, k{0};
    cin >> M;
    while( grad_f(x) ){

    }


    return 0;
}