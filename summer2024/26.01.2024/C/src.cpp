#include <bits/stdc++.h>
#define MAXN 110

int questions[MAXN];

int main( ){
    int testCases{0}, correct{0};
    char answer{0}, read{0};
    std::cin >> testCases;
    int numQuestions{0}, numCorrectedPapers{0};
    while( testCases-- ){
        std::cin >> numQuestions >> numCorrectedPapers;
        for( int i{0}; i < numCorrectedPapers; i++ ){
            for( int j{0}; j < numQuestions; i++ ){
                std::cin >> answer >> read;
                

            }
        }
    }

    return 0;
}