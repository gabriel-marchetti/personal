#include <bits/stdc++.h>

using namespace std;

void addTeam( vector<int> students ){

}

int main(){
    int num_students, read_student;
    cin >> num_students;
    vector<int> students;
    for(int i{0}; i < num_students; i++){
        cin >> read_student;
        students.push_back(read_student);
    }



    return 0;
}